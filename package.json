{
  "name": "discord-bun-template",
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "bun --watch .",
    "start": "bun .",
    "docker": "docker compose build && docker compose up -d"
  },
  "dependencies": {
    "commandkit": "^0.1.10",
    "discord.js": "^14.14.1",
    "mongoose": "^8.3.1"
  }
}
